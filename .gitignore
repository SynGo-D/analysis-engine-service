__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
dist/
build/
.pytest_cache/

# Analyzer toolchain binaries — not source, installed via tools/eslint's
# own npm install and the reviewdog install script (see README.md).
# package-lock.json IS committed (reproducible installs), only
# node_modules/ itself is ignored.
.tools-bin/
tools/eslint/node_modules/
